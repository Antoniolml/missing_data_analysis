# conda env create --file environment.yml
# mamba env create --file environment.yml
name: missing
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - black
  - invoke
  - jupyter
  - jupyterlab
  - matplotlib==3.5.1
  - nbdime
  - numpy
  - pandas
  - pip
  - plotly
  - pyjanitor
  - pyprojroot
  - python=3.9
  - seaborn
  - missingno
  - pyreadr
  - session-info
  - upsetplot==0.6.1
